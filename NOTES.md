
```
npm init -y
npm install typescript
npx tsc --init

npx tsc (convert typescript to javascript)
npm run build

npm whoami (check which account logged in)
npm login
npx tsc
npm publish --access=public
```

###Patch new version
```
npm version patch
**Our preversion, version, and postversion will run, create a new tag in git and push it to our remote repository. Now publish again: **

npm publish
```

